# Artifact Hub repository metadata
# https://artifacthub.io/docs/topics/repositories/helm-charts/
# repositoryID is generated by Artifact Hub after the repo is submitted via the UI.
# Add it here once the submission is complete.
